class="note-box"><p><strong>Condizioni:</strong> ${esc(p.conditions||'—')}</p><p><strong>Allergie:</strong> ${esc(p.allergies||'—')}</p><p><strong>Farmaci:</strong> ${esc(p.medications||'—')}</p></div><div class="note-box"><p><strong>Preferenze:</strong> ${esc(p.preferences||'—')}</p><p><strong>Note cliniche:</strong> ${esc(p.clinical_notes||'—')}</p></div></div>
      <div class="grid two section-block"><div><h3>Consensi</h3>${c.length?c.map(x=>`<div class="history-row"><div><strong>${esc(x.consent_type)}</strong><small>${dateIt(x.accepted_at||x.created_at)}${x.notes?' · '+esc(x.notes):''}</small></div><span class="status-badge ${x.accepted?'published':'draft'}">${x.accepted?'Accettato':'Non accettato'}</span></div>`).join(''):'<div class="empty-state">Nessun consenso registrato.</div>'}</div><div><h3>Piani recenti</h3>${planMiniList(plans.data||[])}</div></div></div>`;
    bindPatientDetailActions(id);
  }finally{setLoading(false);}
}
function stat(label,value){return `<div class="detail-stat"><span>${label}</span><strong>${esc(value)}</strong></div>`;}
function bindPatientDetailActions(id){
  $('#patientDetail').querySelector('[data-new-visit-for]')?.addEventListener('click',()=>openVisitForm(id));
  $('#patientDetail').querySelector('[data-new-bia-for]')?.addEventListener('click',()=>openBiaForm(id));
  $('#patientDetail').querySelector('[data-new-consent-for]')?.addEventListener('click',()=>openConsentForm(id));
  $('#patientDetail').querySelector('[data-generator-for]')?.addEventListener('click',async()=>{selectedPatient=patients.find(p=>p.id===id);await navigate('generator');});
}
function openPatientForm(p=null){
  openModal(`<h3>${p?'Modifica paziente':'Nuovo paziente'}</h3><form id="patientForm"><div class="form-grid cols-2">
    <label>Nome<input name="first_name" required value="${esc(p?.first_name||'')}"></label><label>Cognome<input name="last_name" required value="${esc(p?.last_name||'')}"></label>
    <label>Data di nascita<input name="birth_date" type="date" value="${esc(p?.birth_date||'')}"></label><label>Sesso<select name="sex"><option value=""></option><option ${p?.sex==='F'?'selected':''}>F</option><option ${p?.sex==='M'?'selected':''}>M</option><option ${p?.sex==='Altro'?'selected':''}>Altro</option></select></label>
    <label>Email<input name="email" type="email" value="${esc(p?.email||'')}"></label><label>Telefono<input name="phone" value="${esc(p?.phone||'')}"></label><label>Altezza (cm)<input name="height_cm" type="number" step="0.1" value="${esc(p?.height_cm||'')}"></label><span></span>
    <label>Condizioni<textarea name="conditions" rows="3">${esc(p?.conditions||'')}</textarea></label><label>Allergie / intolleranze<textarea name="allergies" rows="3">${esc(p?.allergies||'')}</textarea></label>
    <label>Preferenze<textarea name="preferences" rows="3">${esc(p?.preferences||'')}</textarea></label><label>Farmaci<textarea name="medications" rows="3">${esc(p?.medications||'')}</textarea></label>
    <label class="span-2">Note cliniche<textarea name="clinical_notes" rows="4">${esc(p?.clinical_notes||'')}</textarea></label>
  </div><div class="modal-actions"><button type="button" class="btn" data-close-modal>Annulla</button><button class="btn primary" type="submit">Salva</button></div></form>`);
  $('#patientForm').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);const payload=Object.fromEntries(fd.entries());payload.height_cm=payload.height_cm?Number(payload.height_cm):null;setLoading(true,'Salvataggio paziente','Aggiorno la cartella clinica.');try{let res;if(p)res=await supabase.from('patients').update(payload).eq('id',p.id);else res=await supabase.from('patients').insert({...payload,clinic_id:profile.clinic_id,nutritionist_id:session.user.id});if(res.error)throw res.error;closeModal();await loadPatients();toast('Paziente salvato.');await renderPatients();}catch(err){toast(err.message,'error');}finally{setLoading(false);}});
}

async function renderVisits(){
  $('#pageRoot').innerHTML=`<div class="toolbar"><div class="filters"><select id="visitPatientFilter">${patientOptions()}</select></div><button id="addVisitBtn" class="btn primary">+ Nuova visita</button></div><div id="visitsTable"></div>`;
  const load=async()=>{let q=supabase.from('visits').select('*').order('visit_date',{ascending:false});const pid=val('#visitPatientFilter');if(pid)q=q.eq('patient_id',pid);const {data,error}=await q.limit(200);if(error)throw error;$('#visitsTable').innerHTML=visitTable(data||[]);}; await load(); $('#visitPatientFilter').addEventListener('change',load); $('#addVisitBtn').addEventListener('click',()=>openVisitForm(val('#visitPatientFilter')||''));
}
function openVisitForm(patientId=''){
  openModal(`<h3>Nuova visita</h3><form id="visitForm"><div class="form-grid cols-2"><label>Paziente<select name="patient_id" required><option value="">Seleziona…</option>${patients.map(p=>`<option value="${p.id}" ${p.id===patientId?'selected':''}>${esc(p.last_name)} ${esc(p.first_name)}</option>`).join('')}</select></label><label>Data<input name="visit_date" type="date" required value="${today()}"></label><label>Peso (kg)<input name="weight_kg" type="number" step="0.1"></label><label>Vita (cm)<input name="waist_cm" type="number" step="0.1"></label><label>Obiettivo<input name="goal"></label><label>Prossima visita<input name="next_visit" type="date"></label><label class="span-2">Note<textarea name="notes" rows="4"></textarea></label></div><div class="modal-actions"><button type="button" class="btn" data-close-modal>Annulla</button><button class="btn primary" type="submit">Registra visita</button></div></form>`);
  $('#visitForm').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.currentTarget);const p=Object.fromEntries(fd.entries());['weight_kg','waist_cm'].forEach(k=>p[k]=p[k]?Number(p[k]):null);p.next_visit=p.next_visit||null;p.created_by=session.user.id;setLoading(true);try{const {error}=await supabase.from('visits').insert(p);if(error)throw error;closeModal();toast('Visita registrata.');if(currentPage==='visits')await renderVisits();}catch(err){toast(err.message,'error');}finally{setLoading(false);}});
}

async function renderBia(){
  $('#pageRoot').innerHTML=`<div class="toolbar"><div class="filters"><select id="biaPatientFilter">${patientOptions()}</select></div><button id="addBiaBtn" class="btn primary">+ Nuova BIA</button></div><div id="biaTable"></div>`;
  const load=async()=>{let q=supabase.from('bia_records').select('*').order('measured_at',{ascending:false});const pid=val('#biaPatientFilter');if(pid)q=q.eq('patient_id',pid);const {data,error}=await q.limit(200);if(error)throw error;const rows=data||[];$('#biaTable').innerHTML=rows.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>Data</th><th>Paziente</th><th>Peso</th><th>FM %</th><th>FFM</th><th>Muscolo</th><th>Acqua</th><th>PhA</th><th>BMR</th></tr></thead><tbody>${rows.map(b=>`<tr><td>${dateIt(b.measured_at)}</td><td>${esc(patientName(b.patient_id))}</td><td>${b.weight_kg?fmt(b.weight_kg,1)+' kg':'—'}</td><td>${b.body_fat_pct?fmt(b.body_fat_pct,1)+'%':'—'}</td><td>${b.fat_free_mass_kg?fmt(b.fat_free_mass_kg,1)+' kg':'—'}</td><td>${b.muscle_mass_kg?fmt(b.muscle_mass_kg,1)+' kg':'—'}</td><td>${b.body_water_pct?fmt(b.body_water_pct,1)+'%':'—'}</td><td>${b.phase_angle?fmt(b.phase_angle,1)+'°':'—'}</td><td>${b.bmr_kcal?fmt(b.bmr_kcal)+' kcal':'—'}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty-state">Nessuna analisi BIA.</div>';}; await load(); $('#biaPatientFilter').addEventListener('change',load); $('#addBiaBtn').addEventListener('click',()=>openBiaForm(val('#biaPatientFilter')||''));
}
function openBiaForm(patientId=''){
  openModal(`<h3>Nuova analisi BIA</h3><form id="biaForm"><div class="form-grid cols-3"><label>Paziente<select name="patient_id" required><option value="">Seleziona…</option>${patients.map(p=>`<option value="${p.id}" ${p.id===patientId?'selected':''}>${esc(p.last_name)} ${esc(p.first_name)}</option>`).join('')}</select></label><label>Data<input name="measured_at" type="datetime-local" value="${new Date().toISOString().slice(0,16)}"></label><label>Peso kg<input name="weight_kg" type="number" step="0.01"></label><label>Massa grassa %<input name="body_fat_pct" type="number" step="0.01"></label><label>Massa grassa kg<input name="fat_mass_kg" type="number" step="0.01"></label><label>Massa magra kg<input name="fat_free_mass_kg" type="number" step="0.01"></label><label>Massa muscolare kg<input name="muscle_mass_kg" type="number" step="0.01"></label><label>Acqua corporea %<input name="body_water_pct" type="number" step="0.01"></label><label>Phase angle °<input name="phase_angle" type="number" step="0.01"></label><label>Grasso viscerale<input name="visceral_fat" type="number" step="0.01"></label><label>BMR kcal<input name="bmr_kcal" type="number" step="1"></label><label>Fonte<input name="source_type" value="manual"></label><label class="span-3">Note<textarea name="notes" rows="3"></textarea></label></div><div class="modal-actions"><button type="button" class="btn" data-close-modal>Annulla</button><button class="btn primary" type="submit">Salva BIA</button></div></form>`);
  $('#biaForm').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),p=Object.fromEntries(fd.entries());['weight_kg','body_fat_pct','fat_mass_kg','fat_free_mass_kg','muscle_mass_kg','body_water_pct','phase_angle','visceral_fat','bmr_kcal'].forEach(k=>p[k]=p[k]?Number(p[k]):null);p.measured_at=p.measured_at?new Date(p.measured_at).toISOString():new Date().toISOString();p.created_by=session.user.id;setLoading(true);try{const {error}=await supabase.from('bia_records').insert(p);if(error)throw error;closeModal();toast('Analisi BIA salvata.');if(currentPage==='bia')await renderBia();}catch(err){toast(err.message,'error');}finally{setLoading(false);}});
}
function openConsentForm(patientId){
  openModal(`<h3>Registra consenso</h3><form id="consentForm"><div class="form-grid cols-2"><label>Tipo consenso<input name="consent_type" required placeholder="es. privacy, WhatsApp, condivisione dati"></label><label>Stato<select name="accepted"><option value="true">Accettato</option><option value="false">Non accettato</option></select></label><label>Data<input name="accepted_at" type="date" value="${today()}"></label><label>Scadenza<input name="expires_at" type="date"></label><label class="span-2">Note<textarea name="notes" rows="3"></textarea></label></div><div class="modal-actions"><button type="button" class="btn" data-close-modal>Annulla</button><button class="btn primary">Salva</button></div></form>`);
  $('#consentForm').addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.currentTarget),p=Object.fromEntries(fd.entries());p.patient_id=patientId;p.accepted=p.accepted==='true';p.accepted_at=p.accepted_at?new Date(p.accepted_at).toISOString():null;p.expires_at=p.expires_at||null;p.recorded_by=session.user.id;const {error}=await supabase.from('consents').insert(p);if(error){toast(error.message,'error');return;}closeModal();toast('Consenso registrato.');showPatientDetail(patientId);});
}

async function renderPlans(){
  $('#pageRoot').innerHTML=`<div class="toolbar"><div class="filters"><select id="planPatientFilter">${patientOptions()}</select><select id="planStatusFilter"><option value="">Tutti gli stati</option><option value="draft">Bozze</option><option value="published">Pubblicati</option></select></div><button class="btn primary" data-page="generator">+ Genera nuovo piano</button></div><div id="plansList"></div><div id="globalPlanDetail" class="section-block"></div>`;
  const load=async()=>{let q=supabase.from('diet_plans').select('*').order('created_at',{ascending:false});const pid=val('#planPatientFilter'),status=val('#planStatusFilter');if(pid)q=q.eq('patient_id',pid);if(status)q=q.eq('status',status);const {data,error}=await q.limit(200);if(error)throw error;const rows=data||[];$('#plansList').innerHTML=rows.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>Paziente</th><th>Piano</th><th>Target</th><th>Score</th><th>Stato</th><th>Creato</th><th></th></tr></thead><tbody>${rows.map(p=>`<tr><td>${esc(patientName(p.patient_id))}</td><td><div class="table-primary">${esc(p.title)}</div><div class="table-secondary">${esc(p.goal||'')}</div></td><td>${fmt(p.calories_target)} kcal</td><td>${p.quality_score?fmt(p.quality_score,1):'—'}</td><td><span class="status-badge ${p.status}">${esc(p.status)}</span></td><td>${dateIt(p.created_at)}</td><td><button class="btn small" data-open-plan-global="${p.id}">Apri</button></td></tr>`).join('')}</tbody></table></div>`:'<div class="empty-state">Nessun piano.</div>';}; await load(); $('#planPatientFilter').addEventListener('change',load);$('#planStatusFilter').addEventListener('change',load);
}
async function openPlanFromAny(planId){
  if(currentPage!=='plans') await navigate('plans'); const target=$('#globalPlanDetail'); if(!target)return; target.innerHTML='<div class="card empty-state">Caricamento piano…</div>'; const {html,plan}=await getPlanMarkup(planId,false); currentPlanId=planId; target.innerHTML=`<div class="section-title"><h3>Dettaglio piano</h3><div class="actions">${plan.status!=='published'?`<button id="globalPublishBtn" class="btn primary">Pubblica</button>`:''}</div></div>${html}`; $('#globalPublishBtn')?.addEventListener('click',()=>publishPlan(planId,()=>openPlanFromAny(planId)));
}

async function renderFoods(){
  $('#pageRoot').innerHTML=`<div class="toolbar"><div class="filters"><input id="foodSearch" type="search" placeholder="Cerca un alimento…"><select id="foodSource"><option value="">CREA + USDA + Custom</option><option value="CREA">CREA</option><option value="USDA">USDA</option><option value="CUSTOM">Custom</option></select></div><button id="addFoodBtn" class="btn primary">+ Alimento personalizzato</button></div><div id="foodResults"></div>`;
  const load=async()=>{const qtext=val('#foodSearch').trim(),source=val('#foodSource');let q=supabase.from('foods').select('id,name,source,category,subcategory,serving_g,calories,protein_g,carbs_g,fat_g,fiber_g,sodium_mg,generator_eligible').order('source_priority').order('name');if(source)q=q.eq('source',source);if(qtext)q=q.ilike('name',`%${qtext}%`);else q=q.eq('generator_eligible',true);const {data,error}=await q.limit(80);if(error)throw error;const rows=data||[];$('#foodResults').innerHTML=rows.length?`<div class="food-search-results">${rows.map(f=>`<div class="food-result"><div><strong>${esc(f.name)}</strong><small>${esc(f.category||'—')} ${f.subcategory&&f.subcategory!==f.category?'· '+esc(f.subcategory):''} · ${fmt(f.calories)} kcal/100g · P ${fmt(f.protein_g,1)} · C ${fmt(f.carbs_g,1)} · G ${fmt(f.fat_g,1)} · Fibra ${fmt(f.fiber_g,1)}</small></div><span class="source-chip ${f.source}">${esc(f.source)}</span></div>`).join('')}</div>`:'<div cl