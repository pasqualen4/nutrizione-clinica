ion generationBody(){const mealShares=getMealShares();return{patient_id:selectedPatient.id,title:val('#planTitle'),goal:val('#goal'),days:num('#days'),calories_target:num('#calories'),protein_target_g:num('#protein'),carbs_target_g:num('#carbs'),fat_target_g:num('#fat'),fiber_target_g:num('#fiber'),sodium_limit_mg:num('#sodium')||null,bmr_kcal:num('#bmr')||null,tdee_kcal:num('#tdee')||null,activity_factor:num('#activityFactor')||null,dietary_pattern:val('#dietaryPattern'),meals:['breakfast','morning_snack','lunch','afternoon_snack','dinner'].map(type=>({type,share:mealShares[type]})),meal_shares:mealShares,allergies:splitList(val('#allergiesExtra')),excluded_foods:splitList(val('#excludedFoods')),preferences:splitList(val('#preferences')),clinical_notes:val('#clinicalNotes'),patient_notes:val('#patientNotes'),weekly_frequency:getFrequency()};}
async function edgeError(error){try{const r=error?.context;if(r){const p=await(r.clone?r.clone():r).json();return p?.error||p?.message||error.message;}}catch{}return error?.message||String(error);}
async function generatePlan(){if(!selectedPatient){toast('Seleziona prima un paziente.','error');return;}const total=$$('.meal-share').reduce((a,x)=>a+Number(x.value||0),0);if(total!==100){toast('La distribuzione dei pasti deve sommare 100%.','error');return;}setLoading(true,'Generazione in corso','Ottimizzo pasti, porzioni, varietà ed equivalenze.');try{const {data,error}=await supabase.functions.invoke('generate-diet-plan',{body:generationBody()});if(error)throw new Error(await edgeError(error));if(data?.error)throw new Error(data.error);currentPlanId=data.plan_id;toast(`Piano generato · score ${fmt(data.quality_score,1)}%`);await loadGeneratorPlan(currentPlanId);await loadRecentGeneratorPlans();}catch(e){toast(e.message,'error');}finally{setLoading(false);}}
async function loadGeneratorPlan(id){const {html,plan}=await getPlanMarkup(id,false);currentPlanId=id;$('#planEmpty').classList.add('hidden');$('#planOutput').classList.remove('hidden');$('#planActions').classList.remove('hidden');$('#publishPlanBtn').textContent=plan.status==='published'?'✓ Piano pubblicato':'Pubblica al paziente';$('#publishPlanBtn').disabled=plan.status==='published';$('#planOutput').innerHTML=html;}
async function getPlanMarkup(planId,readOnly=true){setLoading(true,'Caricamento piano','Recupero pasti, metriche e nuove equivalenze.');try{const [planRes,metricsRes,mealsRes,eqRes]=await Promise.all([supabase.from('diet_plans').select('*').eq('id',planId).single(),supabase.from('diet_plan_day_metrics').select('*').eq('plan_id',planId).order('day_number'),supabase.from('plan_meals').select('id,day_number,meal_type,sort_order,preparation_title,instructions,is_free_meal,plan_food_items(id,food_id,food_name,quantity_g,unit,calories,protein_g,carbs_g,fat_g,fiber_g,sodium_mg,food_category,slot_key)').eq('plan_id',planId).order('day_number').order('sort_order'),supabase.rpc('get_plan_equivalent_candidates',{p_plan_id:planId,p_limit:3})]);if(planRes.error)throw planRes.error;if(metricsRes.error)throw metricsRes.error;if(mealsRes.error)throw mealsRes.error;if(eqRes.error)throw eqRes.error;const eqMap={};(eqRes.data||[]).forEach(x=>(eqMap[x.base_item_id]??=[]).push(x));return{html:planMarkup(planRes.data,metricsRes.data||[],mealsRes.data||[],eqMap,readOnly),plan:planRes.data};}finally{setLoading(false);}}
function planMarkup(plan,metrics,meals,eqMap,readOnly){const byDay=Object.fromEntries(metrics.map(m=>[m.day_number,m])),grouped={};meals.forEach(m=>(grouped[m.day_number]??=[]).push(m));const days=[...new Set([...metrics.map(x=>x.day_number),...meals.map(x=>x.day_number)])].sort((a,b)=>a-b),warnings=[...new Set(metrics.flatMap(m=>m.warnings||[]))],score=Math.round(Number(plan.quality_score||0)*10)/10;return `<div class="plan-hero"><div class="card score-card"><div class="score-ring" style="--score:${Math.max(0,Math.min(100,score))}"><strong>${fmt(score,1)}</strong></div><span>Quality Score / 100</span></div><div class="card plan-summary-card"><p class="eyebrow">${esc(plan.status)}</p><h3>${esc(plan.title)}</h3><p class="muted">${esc(plan.goal||'Piano nutrizionale personalizzato')}</p><div class="plan-meta"><span>${fmt(plan.calories_target)} kcal</span><span>${fmt(plan.protein_target_g)}g P</span><span>${fmt(plan.carbs_target_g)}g C</span><span>${fmt(plan.fat_target_g)}g G</span><span>Algoritmo ${esc(plan.algorithm_version||'2.1')}</span></div>${warnings.length?`<div class="warning-list">${warnings.map(w=>`<div class="warning">⚠ ${esc(w)}</div>`).join('')}</div>`:''}</div></div><div class="day-list">${days.map(d=>renderDay(d,byDay[d],grouped[d]||[],eqMap,readOnly)).join('')}</div>`;}
function renderDay(day,metric,meals,eqMap,readOnly){const dm=metric?`<span>${fmt(metric.calories)} kcal</span><span>P ${fmt(metric.protein_g)}g</span><span>C ${fmt(metric.carbs_g)}g</span><span>G ${fmt(metric.fat_g)}g</span><span>Score ${fmt(metric.quality_score,1)}</span>`:'';return `<article class="day-card"><div class="day-head"><div><p class="eyebrow">Giorno ${day}</p><h4>${dayLabels[day]||`Giorno ${day}`}</h4></div><div class="day-metrics">${dm}${!readOnly?`<button class="btn small" data-regen-day="${day}">↻ Rigenera</button>`:''}</div></div><div class="day-body">${meals.map(m=>renderMeal(m,eqMap)).join('')}</div></article>`;}
function renderMeal(meal,eqMap){const items=meal.plan_food_items||[],total=items.reduce((a,x)=>a+Number(x.calories||0),0);return `<div class="meal-row"><div class="meal-title"><strong>${mealLabels[meal.meal_type]||esc(meal.meal_type)}</strong><span class="muted">${fmt(total)} kcal</span></div>${items.map(i=>{const eq=eqMap[i.id]||[];return `<div class="food-item"><div class="food-name"><strong>${esc(i.food_name)}</strong><small>${esc(i.food_category||'')} · ${fmt(i.calories)} kcal · P ${fmt(i.protein_g,1)} · C ${fmt(i.carbs_g,1)} · G ${fmt(i.fat_g,1)}</small>${eq.length?`<div class="equivalents">${eq.map(e=>`<span>oppure ${esc(e.food_name)} ${fmt(e.quantity_g)}g · ${esc(e.source)}</span>`).join('')}</div>`:''}</div><div class="quantity">${fmt(i.quantity_g)} ${esc(i.unit||'g')}</div></div>`;}).join('')}</div>`;}
async function regenerateDay(day){if(!currentPlanId)return;setLoading(true,`Rigenero ${dayLabels[day]||'il giorno'}`,'Mantengo target e varietà settimanale.');try{const {data,error}=await supabase.functions.invoke('regenerate-diet-day',{body:{plan_id:currentPlanId,day_number:day}});if(error)throw new Error(await edgeError(error));if(data?.error)throw new Error(data.error);toast('Giorno rigenerato.');await loadGeneratorPlan(currentPlanId);}catch(e){toast(e.message,'error');}finally{setLoading(false);}}
async function publishPlan(id=currentPlanId,after=()=>{}){if(!id)return;if(!confirm('Pubblicare questo piano al paziente?'))return;const {error}=await supabase.from('diet_plans').update({status:'published',published_at:new Date().toISOString()}).eq('id',id);if(error){toast(error.message,'error');return;}toast('Piano pubblicato.');await after();}
async function loadRecentGeneratorPlans(){if(!selectedPatient)return;const {data,error}=await supabase.from('diet_plans').select('*').eq('patient_id',selectedPatient.id).order('created_at',{ascending:false}).limit(10);if(error)return;$('#recentPlans').className='card';$('#recentPlans').innerHTML=(data||[]).length?`<div class="history-list">${data.map(p=>`<div class="history-row"><div><strong>${esc(p.title)}</strong><small>${dateTimeIt(p.created_at)} · ${fmt(p.calories_target)} kcal</small></div><span class="status-badge ${p.status}">${esc(p.status)}</span><button class="btn small" data-open-generator-plan="${p.id}">Apri</button></div>`).join('')}</div>`:'<div class="empty-state">Nessun piano.</div>';}
function resetGeneratorPlan(){if(!$('#planOutput'))return;$('#planOutput').classList.add('hidden');$('#planOutput').innerHTML='';$('#planEmpty').classList.remove('hidden');$('#planActions').classList.add('hidden');}

async function renderPatientHome(){if(!selfPatient){$('#pageRoot').innerHTML='<div class="card empty-state">Il tuo account non è ancora collegato a una cartella paziente.</div>';return;}const [visit,bia,plans,docs]=await Promise.all([supabase.from('visits').select('*').eq('patient_id',selfPatient.id).order('visit_date',{ascending:false}).limit(1).maybeSingle(),supabase.from('bia_records').select('*').eq('patient_id',selfPatient.id).order('measured_at',{ascending:false}).limit(1).maybeSingle(),supabase.from('diet_plans').select('id',{count:'exact',head:true}).eq('patient_id',selfPatient.id).eq('status','published'),supabase.from('documents').select('id',{count:'exact',head:true}).eq('patient_id',selfPatient.id).eq('visible_to_patient',true)]);const v=visit.data,b=bia.data;$('#pageRoot').innerHTML=`<div class="card"><div class="card-title"><div><p class="eyebrow">Benvenuto</p><h3>${esc(selfPatient.first_name)} ${esc(selfPatient.last_name)}</h3></div></div><div class="grid four">${kpi('Ultimo peso',b?.weight_kg||v?.weight_kg?`${fmt(b?.weight_kg||v?.weight_kg,1)} kg`:'—','')}${kpi('Massa grassa',b?.body_fat_pct?`${fmt(b.body_fat_pct,1)}%`:'—','')}${kpi('Piani disponibili',plans.count||0,'Pubblicati')}${kpi('Documenti',docs.count||0,'Condivisi')}</div></div><div class="grid two section-block"><div class="card"><h3>Ultima visita</h3><p class="muted">${v?`${dateIt(v.visit_date)} · ${esc(v.goal||'Controllo')}`:'Nessuna visita registrata.'}</p>${v?.notes?`<div class="note-box">${esc(v.notes)}</div>`:''}</div><div class="card"><h3>Ultima BIA</h3><p class="muted">${b?`${dateIt(b.measured_at)} · BMR ${fmt(b.bmr_kcal)} kcal`:'Nessuna BIA disponibile.'}</p></div></div>`;}
async function renderPatientPlans(){if(!selfPatient)return;const {data,error}=await supabase.from('diet_plans').select('*').eq('patient_id',selfPatient.id).eq('status','published').order('published_at',{ascending:false});if(error)throw error;const rows=data||[];$('#pageRoot').innerHTML=`<div>${rows.length?`<div class="history-list">${rows.map(p=>`<div class="history-row"><div><strong>${esc(p.title)}</strong><small>${dateIt(p.published_at)} · ${fmt(p.calories_target)} kcal</small></div><span class="status-badge published">Pubblicato</span><button class="btn small" data-patient-open-plan="${p.id}">Apri</button></div>`).join('')}</div>`:'<div class="card empty-state">Nessun piano pubblicato.</div>'}</div><div id="patientPlanDetail" class="section-block"></div>`;$('#pageRoot').querySelectorAll('[data-patient-open-plan]').forEach(b=>b.addEventListener('click',async()=>{const {html}=await getPlanMarkup(b.dataset.patientOpenPlan,true);$('#patientPlanDetail').innerHTML=html;}));}
async function renderPatientBia(){if(!selfPatient)return;const {data,error}=await supabase.from('bia_records').select('*').eq('patient_id',selfPatient.id).order('measured_at',{ascending:false});if(error)throw error;const rows=data||[];$('#pageRoot').innerHTML=rows.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>Data</th><th>Peso</th><th>Massa grassa</th><th>Massa magra</th><th>Muscolo</th><th>Acqua</th><th>Phase angle</th><th>BMR</th></tr></thead><tbody>${rows.map(b=>`<tr><td>${dateIt(b.measured_at)}</td><td>${b.weight_kg?fmt(b.weight_kg,1)+' kg':'—'}</td><td>${b.body_fat_pct?fmt(b.body_fat_pct,1)+'%':'—'}</td><td>${b.fat_free_mass_kg?fmt(b.fat_free_mass_kg,1)+' kg':'—'}</td><td>${b.muscle_mass_kg?fmt(b.muscle_mass_kg,1)+' kg':'—'}</td><td>${b.body_water_pct?fmt(b.body_water_pct,1)+'%':'—'}</td><td>${b.phase_angle?fmt(b.phase_angle,1)+'°':'—'}</td><td>${b.bmr_kcal?fmt(b.bmr_kcal)+' kcal':'—'}</td></tr>`).join('')}</tbody></table></div>`:'<div class="card empty-state">Nessuna analisi BIA disponibile.</div>';}
async function renderPatientDocuments(){if(!selfPatient)return;const {data,error}=await supabase.from('documents').select('*').eq('patient_id',selfPatient.id).eq('visible_to_patient',true).order('created_at',{ascending:false});if(error)throw error;const rows=data||[];$('#pageRoot').innerHTML=rows.length?`<div class="history-list">${rows.map(d=>`<div class="history-row"><div><strong>${esc(d.filename)}</strong><small>${esc(d.document_type||'Documento')} · ${dateIt(d.created_at)}</small></div><span class="tag">${esc(d.mime_type||'file')}</span><button class="btn small" data-patient-doc="${esc(d.storage_path)}">Apri</button></div>`).join('')}</div>`:'<div class="card empty-state">Nessun documento condiviso.</div>';$('#pageRoot').querySelectorAll('[data-patient-doc]').forEach(b=>b.addEventListener('click',()=>downloadDocument(b.dataset.patientDoc)));}

init();
